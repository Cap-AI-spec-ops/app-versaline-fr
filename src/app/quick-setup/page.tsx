import QuickSetupPanel from "@/components/quick-setup-panel";

export const metadata = {
  title: "Quick Setup – Versaline",
};

export default function QuickSetupPage() {
  return <QuickSetupPanel />;
}
